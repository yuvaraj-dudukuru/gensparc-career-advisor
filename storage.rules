rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Locked down for MVP - no storage access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
